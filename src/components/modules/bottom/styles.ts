import { styled } from '@stitches/react';

export const BottomWrapper = styled('div', {
  width: '100%',
  height: '80px',

  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',

  p: {
    color: '$text',
  },
});
